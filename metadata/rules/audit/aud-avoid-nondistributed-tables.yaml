# Canonical source: vault 20-engine/rules/规则文档/分布式数据库不建议创建非分布表.md
# TODO(产品核对): aud-avoid-nondistributed-tables → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-avoid-nondistributed-tables
category: ddl
severity: warning
tags:
- review:CREATE TABLE`（分布式环境）
content:
  zh:
    name: 分布式数据库不建议创建非分布表
    description: |-
      在分布式数据库中，非分布表（如 `DISTRIBUTED BY LOCAL`）的数据仅存储在单个节点上，容易成为性能瓶颈和单点故障。当多个查询同时访问该表时，所有请求都会集中到一个节点，无法充分利用分布式架构的并行计算能力。
      此外，非分布表与分布表进行关联操作时，通常需要将分布表的数据重分布到非分布表所在的节点，产生大量的网络传输开销。建议所有业务表均使用合理的分布策略，充分利用分布式数据库的水平扩展优势。
    badExample: |-
      -- ❌ 不推荐：在分布式环境中创建本地表/非分布表
      CREATE TABLE large_table (
          id   BIGINT,
          data TEXT
      ) DISTRIBUTED BY LOCAL;
    goodExample: |-
      -- ✅ 推荐：使用 HASH 分布，数据均匀分散到各节点
      CREATE TABLE large_table (
          id   BIGINT,
          data TEXT
      ) DISTRIBUTED BY HASH (id);
  en:
    name: Avoid Non-Distributed Tables
