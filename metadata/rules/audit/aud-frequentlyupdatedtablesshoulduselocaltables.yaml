# Canonical source: vault 20-engine/rules/规则文档/经常更新的表应使用本地表.md
# TODO(产品核对): aud-frequentlyupdatedtablesshoulduselocaltables → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-frequentlyupdatedtablesshoulduselocaltables
category: dml
severity: info
tags:
- review:TABLE`（巡检评估）
content:
  zh:
    name: 经常更新的表应使用本地表
    description: |-
      对于高并发且包含频繁 UPDATE 或 SELECT ... FOR UPDATE 操作的表，如果使用分布式/分片表，跨分片的更新和加锁会严重拖垮性能，引发分布式事务开销、锁传播延迟和网络消耗。
      如果业务场景可以接受，建议将此类表设计为本地表（非分片表/LOCAL 表），以避免分布式锁的开销和跨节点事务的复杂度。如果必须使用分布式表，应仔细评估分片键的选择，确保更新操作能够路由到单分片。
    badExample: |-
      -- ❌ 不推荐：高频更新的表使用分片表，且更新操作不带分片键
      UPDATE t_sharded SET status = 'completed' WHERE other_col = ?;
    goodExample: |-
      -- ✅ 推荐：评估是否可设计为本地表
      CREATE TABLE t_local (...) ENGINE=InnoDB;
      -- 或确保更新查询包含分片键
      UPDATE t_sharded SET status = 'completed' WHERE shard_key = ?;
  en:
    name: FrequentlyUpdatedTablesShouldUseLocalTables
