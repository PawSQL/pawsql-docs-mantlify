# Canonical source: vault 20-engine/rules/规则文档/禁止更新索引中的列.md
# TODO(产品核对): aud-updatingcolumnsusedinindexdisallowed → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-updatingcolumnsusedinindexdisallowed
category: ddl
severity: warning
tags:
- review:ALTER TABLE MODIFY COLUMN
content:
  zh:
    name: 禁止更新索引中的列
    description: |-
      应避免频繁更新索引列，因为这会触发索引项删除与重建导致随机 I/O、页分裂与锁竞争显著增加，从而放大写放大与性能抖动。对索引列的每次更新都意味着数据库不仅要修改数据页，还要同步维护所有包含该列的索引结构，在高并发场景下可能造成严重的性能瓶颈。
      合理做法是将高变更字段从索引中拆出，或改为对低频更新字段建索引，必要时用覆盖查询或冗余列替代。此规则适用于 ALTER TABLE MODIFY COLUMN 操作。
    badExample: |-
      -- ❌ 不推荐：被索引包含的列不应被更新
      ALTER TABLE orders MODIFY COLUMN order_date DATE NOT NULL;
      -- 假设 order_date 已被某个索引包含，此操作可能影响索引结构
    goodExample: -- ✅ 推荐：评估是否可以先删除索引再修改，或新建列进行迁移
  en:
    name: UpdatingColumnsUsedInIndexDisallowed
