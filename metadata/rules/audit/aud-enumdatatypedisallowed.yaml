# Canonical source: vault 20-engine/rules/规则文档/禁止使用ENUM数据类型.md
# TODO(产品核对): aud-enumdatatypedisallowed → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-enumdatatypedisallowed
category: ddl
severity: warning
tags:
- review:CREATE TABLE
- review:ALTER TABLE ADD COLUMN
content:
  zh:
    name: 禁止使用ENUM数据类型
    description: |-
      禁止使用 ENUM 数据类型。ENUM 不是 SQL 标准类型，在不同数据库间的移植性极差。后期如需修改或新增枚举值，通常需要重建整张表（`ALTER TABLE ... MODIFY` 可能触发全表复制），代价巨大。此外，ENUM 的排序基于内部枚举索引而非字面量值，可能导致非预期的排序结果，带来隐蔽的业务逻辑错误。
      推荐使用 `VARCHAR` 配合应用层枚举校验，或使用 `TINYINT` 配合字典表来管理枚举值。这样既保持了数据约束能力，又避免了数据库层面的耦合和移植风险。
    badExample: |-
      -- ❌ 不推荐：ENUM 类型，扩展性差，非 SQL 标准
      CREATE TABLE t (
          status ENUM('active', 'inactive')
      );
    goodExample: |-
      -- ✅ 推荐：使用 VARCHAR 配合应用层校验
      CREATE TABLE t (
          status VARCHAR(20)
      );

      -- ✅ 推荐：使用 TINYINT 配合字典表
      CREATE TABLE t (
          status TINYINT NOT NULL
      );
  en:
    name: EnumDataTypeDisallowed
