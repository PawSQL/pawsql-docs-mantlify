# Canonical source: vault 20-engine/rules/规则文档/时间列未设置默认值或默认值为空.md
# TODO(产品核对): aud-default-value-required-for-time-columns → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-default-value-required-for-time-columns
category: ddl
severity: warning
tags:
- review:CREATE TABLE
- review:ALTER TABLE ADD COLUMN
content:
  zh:
    name: 时间列未设置默认值或默认值为空
    description: |-
      时间列（DATE、DATETIME、TIMESTAMP类型）应当设置合理的默认值（如`CURRENT_TIMESTAMP`），以避免出现全为0的日期格式（如`0000-00-00 00:00:00`）与业务预期不符。未设置默认值的时间列，在INSERT语句未显式提供该列值时，会被填入`NULL`或数据库系统默认的零值，这在业务逻辑中可能导致判断错误或显示异常。
      该规则检查CREATE TABLE和ALTER TABLE ADD COLUMN语句中时间类型列的定义，确保时间列配置了非空的默认值。
    badExample: |-
      -- ❌ 不推荐：时间列未设置默认值，插入时可能产生零日期或NULL
      CREATE TABLE event_log (
          id INT,
          event_time DATETIME
      );
    goodExample: |-
      -- ✅ 推荐：为时间列设置合理的默认值
      CREATE TABLE event_log (
          id INT,
          event_time DATETIME DEFAULT CURRENT_TIMESTAMP
      );
  en:
    name: Default Value Required for Time Columns
