# Canonical source: vault 20-engine/rules/规则文档/唯一性索引命名规范.md
# TODO(产品核对): aud-unique-key-naming-convention → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-unique-key-naming-convention
category: index
severity: info
tags:
- review:CREATE INDEX
- review:CREATE TABLE
- review:ALTER TABLE ADD CONSTRAINT
content:
  zh:
    name: 唯一性索引命名规范
    description: |-
      唯一性索引命名规范要求数据库对象名称遵循统一规范，便于团队协作和代码维护。通常要求唯一索引以 `uk_` 为前缀，后跟表名和列名列表，如 `uk_{table}_{column_list}`。
      规范的命名可以让开发人员和 DBA 快速识别约束类型、归属表和涉及的字段，在故障排查、性能优化和数据库结构变更时大幅提升效率。
    badExample: |-
      -- ❌ 不推荐：唯一索引名称不符合命名规范
      CREATE UNIQUE INDEX idx_email ON t_user (email);
    goodExample: |-
      -- ✅ 推荐：唯一索引名称符合规范（格式: uk_{table}_{columns}）
      CREATE UNIQUE INDEX uk_t_user_email ON t_user (email);
  en:
    name: Unique Key Naming Convention
