# Canonical source: vault 20-engine/rules/规则文档/索引中的字段数目超过阈值.md
# TODO(产品核对): aud-numberofcolumnsinindexexceedthreshold → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-numberofcolumnsinindexexceedthreshold
category: index
severity: warning
tags:
- review:CREATE INDEX
- review:ALTER TABLE ADD INDEX
content:
  zh:
    name: 索引中的字段数目超过阈值
    description: |-
      限制索引中字段的数量，以避免索引过大导致的维护问题和性能下降。复合索引的字段越多，索引树越宽、体积越大，每次 INSERT/UPDATE/DELETE 操作的维护成本越高，且 B+Tree 的查找效率也会随之递减。
      建议将复合索引的列数控制在合理范围内（默认阈值 5），超过阈值的索引需要评估是否可以通过拆分索引或调整查询方式来优化。此阈值可根据业务需求配置。
    badExample: |-
      -- ❌ 不推荐：复合索引字段过多（超过阈值 5）
      CREATE INDEX idx_complex ON orders(a, b, c, d, e, f, g);
    goodExample: |-
      -- ✅ 推荐：将索引拆分为更聚焦的多个索引
      CREATE INDEX idx_orders_a_b ON orders(a, b);
      CREATE INDEX idx_orders_c_d ON orders(c, d);
  en:
    name: NumberOfColumnsInIndexExceedThreshold
