# Canonical source: vault 20-engine/rules/规则文档/禁止对非整形常量进行ORDER BY.md
# TODO(产品核对): aud-orderbyconstexprdisallowed → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-orderbyconstexprdisallowed
category: dml
severity: warning
tags:
- review:SELECT
content:
  zh:
    name: 禁止对非整形常量进行ORDER BY
    description: |-
      对非整数常量进行排序实际上没有意义——这种写法通常是开发者在编写 SQL 时误将列名或位置指示符写成了字符串常量。与 `GROUP BY` 类似，PostgreSQL 中这种写法会语法报错，但在 MySQL 中却不会，返回的结果看似正确但排序逻辑完全错误，难以在测试阶段发现问题。
      该规则要求在 SQL 审计中检测并禁止在 `ORDER BY` 子句中使用常量值，确保排序字段始终为有效的列名或列位置指示符（整数）。
    badExample: |-
      -- ❌ 不推荐：ORDER BY 常量无意义，MySQL 静默执行但排序无效
      SELECT l_orderkey
      FROM lineitem
      ORDER BY '1';
    goodExample: |-
      -- ✅ 推荐：ORDER BY 使用列名或整数位置
      SELECT l_orderkey
      FROM lineitem
      ORDER BY l_orderkey;
      -- 或
      SELECT l_orderkey
      FROM lineitem
      ORDER BY 1;
  en:
    name: OrderByConstExprDisallowed
