# Canonical source: vault 20-engine/rules/规则文档/删除表_视图_索引时需指定IfExists.md
# TODO(产品核对): aud-if-exists-required-for-dropping-objects → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-if-exists-required-for-dropping-objects
category: ddl
severity: warning
tags:
- review:DROP TABLE
- review:DROP VIEW
- review:DROP INDEX
content:
  zh:
    name: 删除表/视图/索引时需指定 IF EXISTS
    description: |-
      删除表、视图或索引时要求使用 `IF EXISTS` 子句，以避免在对象不存在时 DDL 报错中断发布流程。这是提升 DDL 脚本幂等性和跨环境执行稳定性的重要实践。
      如果不添加 `IF EXISTS`，当对象不存在时 `DROP` 语句会直接报错，导致发布流水线中断。使用 `IF EXISTS` 可以确保脚本在任何环境状态下都能安全执行，同时应配合依赖分析和变更审计防止误删仍被使用的对象。
    badExample: |-
      -- ❌ 不推荐：未使用 IF EXISTS，对象不存在时会报错
      DROP TABLE important_table;
    goodExample: |-
      -- ✅ 推荐：使用 IF EXISTS，确保安全删除
      DROP TABLE IF EXISTS important_table;
  en:
    name: IF EXISTS Required for Dropping Objects
