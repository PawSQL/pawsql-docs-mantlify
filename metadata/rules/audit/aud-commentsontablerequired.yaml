# Canonical source: vault 20-engine/rules/规则文档/表必须有注释.md
# TODO(产品核对): aud-commentsontablerequired → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-commentsontablerequired
category: ddl
severity: warning
tags:
- review:CREATE TABLE
content:
  zh:
    name: 表必须有注释
    description: |-
      要求在创建表时必须为表添加注释（COMMENT），详细的表注释可以帮助开发者和维护者快速理解表的用途、字段含义和业务逻辑，提高代码可读性和团队协作效率。在生产环境中，缺少注释的表在交接和维护时会显著增加沟通成本。
      此规则适用于 MySQL 等支持表注释的数据库。建议在表注释中说明表的业务用途、数据来源和生命周期。
    badExample: |-
      -- ❌ 不推荐：表缺少注释
      CREATE TABLE t (id INT);
    goodExample: |-
      -- ✅ 推荐：创建表时添加注释
      CREATE TABLE t (id INT) COMMENT = '用户信息表，记录系统注册用户的基本资料';
  en:
    name: CommentsOnTableRequired
