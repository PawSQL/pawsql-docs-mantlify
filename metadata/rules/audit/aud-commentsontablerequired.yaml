# Canonical source: vault 20-engine/rules/规则文档/表必须有注释.md
# TODO(产品核对): aud-commentsontablerequired → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(复核): en 正文为 zh（vault 源）的忠实翻译，发布前建议母语/产品复核；Rule ID/category/severity 仍待注册表定稿。


id: aud-commentsontablerequired
category: ddl
severity: warning
tags:
- review:CREATE TABLE
content:
  zh:
    name: 表必须有注释
    description: |-
      要求在创建表时必须为表添加注释（COMMENT），详细的表注释可以帮助开发者和维护者快速理解表的用途、字段含义和业务逻辑，提高代码可读性和团队协作效率。在生产环境中，缺少注释的表在交接和维护时会显著增加沟通成本。
      此规则适用于 MySQL 等支持表注释的数据库。建议在表注释中说明表的业务用途、数据来源和生命周期。
    badExample: |-
      -- ❌ 不推荐：表缺少注释
      CREATE TABLE t (id INT);
    goodExample: |-
      -- ✅ 推荐：创建表时添加注释
      CREATE TABLE t (id INT) COMMENT = '用户信息表，记录系统注册用户的基本资料';
    summary: 建表时应写表注释（业务用途/数据来源/生命周期），适用于 MySQL 等支持表注释的库。
  en:
    name: CommentsOnTableRequired
    description: |-
      A table must carry a comment (`COMMENT`) when it is created. A thorough table comment helps developers and maintainers quickly understand the table's purpose, field meanings, and business logic, improving code readability and team collaboration. In production, tables without comments raise communication cost noticeably during handover and maintenance.

      This rule applies to databases that support table comments, such as MySQL. The comment should describe the table's business purpose, data source, and lifecycle.
    summary: CREATE TABLE should carry a table COMMENT (business purpose, data source, lifecycle); applies to engines supporting table comments such as MySQL.
    badExample: |-
      -- bad: the table has no comment
      CREATE TABLE t (id INT);
    goodExample: |-
      -- good: comment on the table at creation
      CREATE TABLE t (id INT) COMMENT = '用户信息表，记录系统注册用户的基本资料';
