# Canonical source: vault 20-engine/rules/规则文档/避免更新唯一约束的值.md
# TODO(产品核对): aud-avoid-updating-unique-constraint-values → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-avoid-updating-unique-constraint-values
category: dml
severity: warning
tags:
- review:UPDATE
content:
  zh:
    name: 避免更新唯一约束的值
    description: |-
      对具有唯一性约束的列进行值更新时，数据库需要对新的值进行全表唯一性检查，以确保更新后的值不与已有数据冲突。在表数据量非常大的情况下，这一唯一性校验操作可能非常昂贵——尤其是在唯一约束列上没有索引或索引效率不高时，可能需要扫描大量数据。
      此外，如果该唯一约束列被其他表作为外键引用，更新操作还会触发级联检查或级联更新，进一步放大性能影响。因此，具有唯一约束的列在业务设计中应尽量保持稳定不变。
    badExample: |-
      -- ❌ 不推荐：更新唯一约束列的值，触发全表唯一性校验
      UPDATE user SET email = 'new_email@example.com' WHERE id = 100;
    goodExample: -- ✅ 推荐：唯一约束列尽量不更新；如必须变更，确保在低峰期执行
  en:
    name: Avoid Updating Unique Constraint Values
