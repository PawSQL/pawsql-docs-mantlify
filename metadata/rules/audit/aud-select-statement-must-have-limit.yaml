# Canonical source: vault 20-engine/rules/规则文档/SELECT 语句必须带LIMIT.md
# TODO(产品核对): aud-select-statement-must-have-limit → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-select-statement-must-have-limit
category: dml
severity: info
tags:
- review:SELECT
content:
  zh:
    name: SELECT 语句必须带LIMIT
    description: |-
      SELECT 语句如果不带 LIMIT 子句，在遇到异常输入参数或表数据量较大时，可能返回海量结果集。服务端对于查询结果的处理能力通常有一定限制，过大的结果集不仅消耗大量网络带宽和内存资源，极端情况下可能导致服务端完全宕机。
      建议为所有生产环境的 SELECT 查询添加 LIMIT 子句，限制单次查询返回的最大行数。PawSQL 检测未添加 LIMIT 的 SELECT 语句并给出预警，帮助开发者建立安全查询的编码习惯。
    badExample: |-
      -- ❌ 不推荐：SELECT 缺少 LIMIT，有全表扫描返回海量数据的风险
      SELECT * FROM user WHERE status = 1;
    goodExample: |-
      -- ✅ 推荐：添加 LIMIT 限制返回行数
      SELECT * FROM user WHERE status = 1 LIMIT 1000;
  en:
    name: SELECT Statement Must Have LIMIT
