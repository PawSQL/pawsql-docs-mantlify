# Canonical source: vault 20-engine/rules/规则文档/索引列上的运算导致索引失效.md
# TODO(产品核对): aud-rulefuncwithcolumninpredicate → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-rulefuncwithcolumninpredicate
category: index
severity: info
tags:
- review:SELECT
content:
  zh:
    name: 索引列上的运算导致索引失效
    description: |-
      在索引列上进行运算（包括函数调用和 `+`、`-`、`*`、`/` 等算术运算符）将导致索引失效，数据库优化器无法使用该索引，容易退化为全表扫描，产生严重的性能问题。
      正确做法是将运算转换到常量端进行，使索引列保持"裸列"状态。PawSQL 可以帮助自动转换大量函数及运算符相关的操作，将索引列上的运算等价移动到常量侧，从而恢复索引可用性。
    badExample: |-
      -- ❌ 不推荐：索引列参与函数运算，导致索引失效
      SELECT * FROM tpch.orders
      WHERE ADDDATE(o_orderdate, INTERVAL 31 DAY) = DATE '2019-10-10';
    goodExample: |-
      -- ✅ 推荐：将运算移至常量端，恢复索引使用
      SELECT * FROM tpch.orders
      WHERE o_orderdate = SUBDATE(DATE '2019-10-10', INTERVAL 31 DAY);
  en:
    name: RuleFuncWithColumnInPredicate
