# Canonical source: vault 20-engine/rules/规则文档/非列表中的列应设置非空默认值.md
# TODO(产品核对): aud-nonnull-default-value-required-for-nonexcluded-c → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-nonnull-default-value-required-for-nonexcluded-c
category: ddl
severity: warning
tags:
- review:CREATE TABLE
- review:ALTER TABLE ADD COLUMN
content:
  zh:
    name: 非列表中的列应设置非空默认值
    description: |-
      对于非大对象类型的列（即不在排除列表中的列），应设置非空默认值以确保数据的完整性。排除列表中的大对象类型（如 JSON、BJSON、XML、BLOB、CLOB、TEXT 等）通常不适合设置默认值，但普通业务字段（如 INT、VARCHAR、DATE 等）如果没有默认值且被定义为 NOT NULL，会导致 INSERT 操作在不指定该列时直接报错，增加应用程序的错误处理负担。
      为这些列设置合理的默认值，可以简化 INSERT 操作、保证数据的完整性，也使得数据库 Schema 更加健壮。默认值应根据业务语义来定义（如状态字段默认 0、时间字段默认当前时间等）。
    badExample: |-
      -- ❌ 不推荐：非大对象类型的列定义为 NOT NULL 但没有默认值
      CREATE TABLE t (
          id INT NOT NULL,
          status INT NOT NULL
      );
    goodExample: |-
      -- ✅ 推荐：为 NOT NULL 列设置合理的默认值
      CREATE TABLE t (
          id INT NOT NULL AUTO_INCREMENT,
          status INT NOT NULL DEFAULT 0
      );
  en:
    name: Non-Null Default Value Required for Non-Excluded Columns
