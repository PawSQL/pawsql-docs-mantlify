# Canonical source: vault 20-engine/rules/规则文档/分区键上的过滤条件禁止使用to_date_to_timestamp函数.md
# TODO(产品核对): aud-no-todatetotimestamp-on-partition-key-in-filter- → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-no-todatetotimestamp-on-partition-key-in-filter-
category: dml
severity: error
tags:
- review:SELECT
content:
  zh:
    name: 分区键上的过滤条件禁止使用 TO_DATE/TO_TIMESTAMP 函数
    description: |-
      在 TDSQL 分布式实例或其他支持分区的数据库中，如果对分区键使用 `TO_DATE` 或 `TO_TIMESTAMP` 等日期转换函数，可能导致分区裁剪失效，使得查询扫描所有分区子表，而非仅扫描目标分区，严重影响查询性能。
      正确的做法是避免对分区键字段应用任何函数，将函数应用于常量一侧，或使用数据库支持的隐式类型转换，以确保优化器能够准确定位目标分区。
    badExample: |-
      -- ❌ 不推荐：对分区键使用日期转换函数，分区裁剪失效
      SELECT * FROM t_log
      WHERE TO_DATE(log_date, 'YYYY-MM-DD') = '2024-01-01';
    goodExample: |-
      -- ✅ 推荐：直接比较或常量侧函数，分区裁剪生效
      SELECT * FROM t_log
      WHERE log_date = '2024-01-01';
  en:
    name: No TO_DATE/TO_TIMESTAMP on Partition Key in Filter Conditions
