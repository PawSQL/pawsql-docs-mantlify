# Canonical source: vault 20-engine/rules/规则文档/整型建议采用 INT(10) 或 BIGINT(20).md
# TODO(产品核对): aud-use-int10-or-bigint20-for-integer-columns → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-use-int10-or-bigint20-for-integer-columns
category: ddl
severity: info
tags:
- review:CREATE TABLE
- review:ALTER TABLE ADD COLUMN
- review:ALTER TABLE MODIFY COLUMN
content:
  zh:
    name: 整型建议采用 INT(10) 或 BIGINT(20)
    description: |-
      在MySQL等数据库中，整型列定义时建议显式指定显示宽度。使用`INT(10)`和`BIGINT(20)`是推荐的标准写法，能够确保整数类型的显示宽度一致，避免在不同客户端或应用程序中出现格式化差异。虽然显示宽度不影响存储范围，但它影响某些客户端（如MySQL命令行）的数据展示效果，统一规范有助于减少视觉歧义。
      该规则检查CREATE TABLE和ALTER TABLE中整型列的定义，要求使用`INT(10)`或`BIGINT(20)`格式，而不是无显示宽度的`INT`或`BIGINT`。
    badExample: |-
      -- ❌ 不推荐：整数列定义缺少显示宽度
      CREATE TABLE t (count INTEGER);
    goodExample: |-
      -- ✅ 推荐：显式指定显示宽度，格式统一规范
      CREATE TABLE t (count INT(10));
  en:
    name: Use INT(10) or BIGINT(20) for Integer Columns
