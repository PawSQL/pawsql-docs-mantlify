# Canonical source: vault 20-engine/rules/规则文档/避免不必要的更新操作.md
# TODO(产品核对): aud-avoid-unnecessary-update-operations → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-avoid-unnecessary-update-operations
category: dml
severity: info
tags:
- review:UPDATE
content:
  zh:
    name: 避免不必要的更新操作
    description: |-
      `UPDATE` 语句即使将字段值更新为与原值完全相同，也会产生写入日志（redo/undo log）并触发相应的行锁、索引维护等开销。在高并发或批量更新的场景下，这些"无效更新"会白白消耗 IO 带宽、日志空间和锁资源，影响整体系统性能。
      通过在 WHERE 条件中加入 `AND col != new_value`，可以过滤掉那些值已为目标值的行，避免无效更新，从而大幅减少 IO 和日志量，提升更新操作的执行效率。
    badExample: |-
      -- ❌ 不推荐：不检查当前值，可能执行无效更新
      UPDATE user SET status = 'ACTIVE' WHERE id = 100;
    goodExample: |-
      -- ✅ 推荐：WHERE 条件中排除已为目标值的行，避免无效更新
      UPDATE user SET status = 'ACTIVE' WHERE id = 100 AND status != 'ACTIVE';
  en:
    name: Avoid Unnecessary UPDATE Operations
