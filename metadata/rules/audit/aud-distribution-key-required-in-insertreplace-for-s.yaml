# Canonical source: vault 20-engine/rules/规则文档/对分片表的INSERT_REPLACE时字段必须包含分布键.md
# TODO(产品核对): aud-distribution-key-required-in-insertreplace-for-s → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-distribution-key-required-in-insertreplace-for-s
category: dml
severity: error
tags:
- review:INSERT
- review:REPLACE`（目标为分片表时）
content:
  zh:
    name: 对分片表的INSERT/REPLACE时字段必须包含分布键
    description: |-
      对于分片表（Sharding Table），执行`INSERT`或`REPLACE`操作时，插入的数据必须显式包含分布键（Shardkey）列及其值。这是因为数据库的Proxy层需要根据分布键的值计算该行数据应该被路由到哪个具体的物理分片（Set）。如果未提供分布键的值，Proxy无法确定数据的目标分片，将直接拒绝执行该SQL语句。
      此规则确保所有写入分片表的数据能够被正确定位和路由到目标分片，避免因缺失分布键而导致的数据路由失败。在TDSQL等分布式数据库中，这是强制执行的基本要求。
    badExample: |-
      -- ❌ 不推荐：未指定分布键id列，Proxy无法确定目标分片
      INSERT INTO t (name) VALUES ('张三');
    goodExample: |-
      -- ✅ 推荐：显式指定分布键id及其值，Proxy可正确路由数据
      INSERT INTO t (id, name) VALUES (100, '张三');
  en:
    name: Distribution Key Required in INSERT/REPLACE for Sharded Tables
