# Canonical source: vault 20-engine/rules/规则文档/禁止删除索引中的列.md
# TODO(产品核对): aud-droppingcolumnsusedinindexdisallowed → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-droppingcolumnsusedinindexdisallowed
category: ddl
severity: warning
tags:
- review:ALTER TABLE DROP COLUMN
content:
  zh:
    name: 禁止删除索引中的列
    description: |-
      禁止删除被索引使用的列。如果某个列是索引定义的一部分，直接删除该列会导致索引失效或被数据库自动重建，引发执行计划变化和性能问题。此外，部分数据库在列被索引引用时可能直接拒绝删除操作，但也有一些数据库会静默删除索引，导致开发者在不知情的情况下丢失重要的性能结构。
      在删除列之前，必须先检查该列是否被索引引用。如果是，应先行删除或重建索引，再安全删除列。
    badExample: |-
      -- ❌ 不推荐：直接删除索引引用的列，导致索引失效
      ALTER TABLE t DROP COLUMN some_col;  -- some_col 被现有索引引用
    goodExample: |-
      -- ✅ 推荐：先处理索引，再删除列
      -- 1. DROP INDEX idx_on_col ON t;  -- 删除依赖索引
      -- 2. ALTER TABLE t DROP COLUMN some_col;
      -- 3. 如有需要，CREATE INDEX 创建替代索引
  en:
    name: DroppingColumnsUsedInIndexDisallowed
