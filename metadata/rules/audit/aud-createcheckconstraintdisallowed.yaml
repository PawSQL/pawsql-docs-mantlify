# Canonical source: vault 20-engine/rules/规则文档/禁止使用CHECK约束.md
# TODO(产品核对): aud-createcheckconstraintdisallowed → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-createcheckconstraintdisallowed
category: ddl
severity: warning
tags:
- review:ALTER TABLE ADD CONSTRAINT
content:
  zh:
    name: 禁止使用CHECK约束
    description: |-
      禁止使用 CHECK 约束，因为在生产环境中 CHECK 约束增加了数据库操作的复杂性和性能开销。每次 INSERT 或 UPDATE 操作都需要对 CHECK 约束的表达式进行求值验证，在大批量写入场景下这会显著拖慢写入性能。此外，CHECK 约束的逻辑隐藏在数据库层，业务代码和数据库之间存在隐式耦合，排查数据质量问题时不易定位。
      推荐将数据校验逻辑上移到应用层，通过业务代码或数据校验框架显式执行检查，这样既保证了数据的合法性校验，又保持了系统的可维护性和可观测性。
    badExample: |-
      -- ❌ 不推荐：定义 CHECK 约束，增加写入开销
      CREATE TABLE t (
          amount DECIMAL CHECK (amount > 0)
      );
    goodExample: |-
      -- ✅ 推荐：在应用层校验数据合法性
      -- 应用层代码示例：if (amount <= 0) throw new ValidationException();
  en:
    name: CreateCheckConstraintDisallowed
