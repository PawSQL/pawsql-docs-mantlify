# Canonical source: vault 20-engine/rules/规则文档/分布式数据库应避免跨分片的DML操作.md
# TODO(产品核对): aud-avoid-crossshard-dml-operations-in-distributed-d → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(复核): en 正文为 zh（vault 源）的忠实翻译，发布前建议母语/产品复核；Rule ID/category/severity 仍待注册表定稿。


id: aud-avoid-crossshard-dml-operations-in-distributed-d
category: dml
severity: warning
tags:
- review:UPDATE
- review:DELETE
content:
  zh:
    name: 分布式数据库应避免跨分片的 DML 操作
    description: |-
      一个 DML 操作（如 `UPDATE` 或 `DELETE`）如果涉及多个分片（即其 `WHERE` 条件未指定分布键），就需要在多个节点上开启分布式事务。分布式事务的性能远低于单节点事务，并且在高并发场景下极易产生分布式死锁。
      为保障分布式数据库的写入性能和事务成功率，应尽量保证每个 DML 事务的影响范围限定在同一个分片内，即 `WHERE` 条件中必须包含分布键的等值条件。
    badExample: |-
      -- ❌ 不推荐：WHERE 条件中不包含分布键，可能跨分片执行
      UPDATE t_user SET status = 1 WHERE user_id = 100;
    goodExample: |-
      -- ✅ 推荐：WHERE 条件包含分布键，事务限定在单个分片
      UPDATE t_user SET status = 1 WHERE id = 1000;
    summary: UPDATE/DELETE 的 WHERE 未含分布键等值会跨分片触发分布式事务（慢且易死锁）；应加分布键等值条件。
  en:
    name: Avoid Cross-Shard DML Operations in Distributed Databases
    description: |-
      A DML operation (such as `UPDATE` or `DELETE`) that touches multiple shards - that is, whose `WHERE` condition does not pin the distribution key - must open a distributed transaction across nodes. Distributed transactions are far slower than single-node transactions and, under high concurrency, easily cause distributed deadlocks.

      To keep write performance and transaction success rates high, keep every DML transaction's impact within a single shard: the `WHERE` clause should include an equality condition on the distribution key.
    summary: DML whose WHERE does not pin the distribution key spans shards and needs slow, deadlock-prone distributed transactions; add a distribution-key equality.
    badExample: |-
      -- bad: WHERE has no distribution key; the update may run across shards
      UPDATE t_user SET status = 1 WHERE user_id = 100;
    goodExample: |-
      -- good: WHERE includes the distribution key so the transaction stays on one shard
      UPDATE t_user SET status = 1 WHERE id = 1000;
