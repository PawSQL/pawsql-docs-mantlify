# Canonical source: vault 20-engine/rules/规则文档/TDSQL中使用ALTER设置自增列的起始值.md
# TODO(产品核对): aud-use-alter-table-to-set-auto-increment-start-in-t → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-use-alter-table-to-set-auto-increment-start-in-t
category: ddl
severity: info
tags:
- review:CREATE TABLE`（TDSQL 分布式环境）
content:
  zh:
    name: TDSQL中使用ALTER设置自增列的起始值
    description: |-
      在 TDSQL 分布式实例中，`CREATE TABLE` 语句中直接指定 `auto_increment` 起始值可能不会生效。这是因为分布式环境下自增列的初始值管理机制与单机 MySQL 不同，`CREATE TABLE` 中的 `AUTO_INCREMENT = N` 参数在分布式架构下无法被正确传递到各分片节点。
      必须使用独立的 `ALTER TABLE ... AUTO_INCREMENT = N` 语句在表创建完成后设置自增列的起始值，确保配置在分布式环境下正确生效。
    badExample: |-
      -- ❌ 不推荐（TDSQL 分布式实例中可能不生效）：CREATE 时指定 auto_increment
      CREATE TABLE t (
          id BIGINT AUTO_INCREMENT PRIMARY KEY
      ) shardkey=id auto_increment=10000;
    goodExample: |-
      -- ✅ 推荐：先用 CREATE 建表，再用 ALTER 设置自增起始值
      CREATE TABLE t (
          id BIGINT AUTO_INCREMENT PRIMARY KEY
      ) shardkey=id;
      ALTER TABLE t AUTO_INCREMENT = 10000;
  en:
    name: Use ALTER TABLE to Set Auto Increment Start in TDSQL
