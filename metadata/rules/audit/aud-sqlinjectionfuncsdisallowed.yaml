# Canonical source: vault 20-engine/rules/规则文档/禁止使用常见 SQL 注入函数.md
# TODO(产品核对): aud-sqlinjectionfuncsdisallowed → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-sqlinjectionfuncsdisallowed
category: dml
severity: warning
tags:
- review:SELECT
- review:INSERT
- review:UPDATE
- review:DELETE
content:
  zh:
    name: 禁止使用常见 SQL 注入函数
    description: |-
      SQL 注入是一种常见的网络攻击技术，它利用不安全的输入验证和构造 SQL 查询来获取未授权的信息或向数据库注入恶意代码。常见的 SQL 注入函数包括 `database()`、`user()`、`version()`、`sleep()`、`load_file()` 等——攻击者通过在输入参数中拼接这些函数，可以获取数据库结构信息、探测注入点是否存在（延时注入）、甚至读取服务器文件。
      该规则要求在 SQL 审计中检测并禁止使用这些高危函数。对于确实需要使用内置函数的场景，应在应用层通过参数化查询完成，而非在 SQL 文本中直接拼接。
    badExample: |-
      -- ❌ 不推荐：SQL 中包含危险注入函数
      SELECT * FROM t WHERE name = CONCAT('input', @@version);

      -- ❌ 不推荐：延时注入探测
      SELECT * FROM t WHERE id = 1 AND SLEEP(5);
    goodExample: |-
      -- ✅ 推荐：使用参数化查询，避免函数注入
      -- PREPARE stmt FROM 'SELECT * FROM t WHERE name = ?';
  en:
    name: SQLInjectionFuncsDisallowed
