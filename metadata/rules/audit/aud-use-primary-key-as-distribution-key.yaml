# Canonical source: vault 20-engine/rules/规则文档/应使用主键作为分布键.md
# TODO(产品核对): aud-use-primary-key-as-distribution-key → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-use-primary-key-as-distribution-key
category: ddl
severity: warning
tags:
- review:CREATE TABLE`（分布式环境）
content:
  zh:
    name: 应使用主键作为分布键
    description: |-
      在分布式数据库中，使用主键作为分布键是最佳实践之一。主键作分布键可以保证数据在各节点间均匀分布，同时依靠主键的唯一性避免数据倾斜。此外，将主键设为分布键还能减少涉及主键操作的跨节点通信——当查询通过主键等值条件定位数据时，可以精确路由到目标节点，避免全节点广播。
      当主键字段未被同时设为分布键时，主键的唯一性只能在各节点内保证，跨节点的全局唯一性需要额外的协调机制，这将增加系统的复杂度和性能开销。
    badExample: |-
      -- ❌ 不推荐：主键为id，但分布键使用其他列，数据分布可能不均
      CREATE TABLE t (
          id BIGINT PRIMARY KEY,
          name VARCHAR(100)
      ) DISTRIBUTED BY HASH(name);
    goodExample: |-
      -- ✅ 推荐：使用主键作为分布键，保证数据均匀分布与全局唯一性
      CREATE TABLE t (
          id BIGINT PRIMARY KEY,
          name VARCHAR(100)
      ) DISTRIBUTED BY HASH(id);
  en:
    name: Use Primary Key as Distribution Key
