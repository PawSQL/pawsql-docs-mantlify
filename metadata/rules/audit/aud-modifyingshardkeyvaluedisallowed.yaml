# Canonical source: vault 20-engine/rules/规则文档/禁止修改分片键的值.md
# TODO(产品核对): aud-modifyingshardkeyvaluedisallowed → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-modifyingshardkeyvaluedisallowed
category: dml
severity: warning
tags:
- review:UPDATE
content:
  zh:
    name: 禁止修改分片键的值
    description: |-
      分片键（Shardkey）的值决定了数据行所在的物理分片。直接更新分片键的值会导致数据需要从一个分片迁移到另一个分片，这在大多数分布式数据库中是不允许的操作，可能导致数据路由混乱、数据丢失或跨分片事务异常。
      如果业务上确实需要修改分片键对应的值，应通过在一个事务中先删除旧记录、再插入新记录的方式显式实现，确保数据迁移的可控性和一致性。
    badExample: |-
      -- ❌ 不推荐：直接修改分片键值，导致跨分片数据迁移
      UPDATE t SET id = 200 WHERE id = 100;
    goodExample: |-
      -- ✅ 推荐：通过 DELETE + INSERT 显式处理分片变更
      BEGIN;
      DELETE FROM t WHERE id = 100;
      INSERT INTO t (id, name, ...) VALUES (200, '...', ...);
      COMMIT;
  en:
    name: ModifyingShardKeyValueDisallowed
