# Canonical source: vault 20-engine/rules/规则文档/自增列的初始值应从0_1开始.md
# TODO(产品核对): aud-initvalue4identitycolumn → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-initvalue4identitycolumn
category: ddl
severity: warning
tags:
- review:CREATE TABLE
content:
  zh:
    name: 自增列的初始值应从0/1开始
    description: |-
      自增列的初始值应当从 0 或 1 开始，以确保生成的键值遵循统一约定。从 0 或 1 开始可以保持一致性，便于理解和管理，避免因初始值不统一导致的混淆——例如某些环境从 1 开始、另一些从 1000 开始，数据迁移和对比时会产生不必要的差异。
      此规则适用于 `CREATE TABLE` 中自增列的定义。默认建议从 1 开始，可通过配置调整。
    badExample: |-
      -- ❌ 不推荐：自增起始值不规范，从 100 开始
      CREATE TABLE t (id INT AUTO_INCREMENT PRIMARY KEY) AUTO_INCREMENT = 100;
    goodExample: |-
      -- ✅ 推荐：自增从 0 或 1 开始
      CREATE TABLE t (id INT AUTO_INCREMENT PRIMARY KEY) AUTO_INCREMENT = 1;
  en:
    name: InitValue4IdentityColumn
