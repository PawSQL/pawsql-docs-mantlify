# Canonical source: vault 20-engine/rules/规则文档/避免表关联字段不是分布键.md
# TODO(产品核对): aud-avoid-joining-on-nondistribution-key-columns → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-avoid-joining-on-nondistribution-key-columns
category: ddl
severity: warning
tags:
- review:SELECT`（分布式环境下的多表 JOIN）
content:
  zh:
    name: 避免表关联字段不是分布键
    description: |-
      在分布式数据库中，如果 JOIN 操作的关联字段不是分布键（Shard Key），关联操作可能需要在多个节点之间移动数据（数据重分布 / Data Redistribution），即将一张表的数据广播或重分布到另一张表所在的节点上才能完成关联计算。这种跨节点的数据传输会产生大量的网络开销，成为查询性能的瓶颈。
      在真实业务环境中，所有关联字段都使用分布键可能不现实，因此需要在表分布策略和查询性能之间进行权衡。应优先确保高频 JOIN 查询的关联字段与分布键对齐，使关联操作能够在本地节点完成，从而充分利用分布式数据库的并行计算能力。
    badExample: |-
      -- ❌ 不推荐：关联字段不是分布键，可能触发跨节点数据重分布
      SELECT * FROM t1 JOIN t2 ON t1.non_shard_key = t2.col;
    goodExample: |-
      -- ✅ 推荐：关联字段使用分布键，关联可在本地节点完成
      SELECT * FROM t1 JOIN t2 ON t1.shard_key = t2.shard_key;
  en:
    name: Avoid Joining on Non-Distribution Key Columns
