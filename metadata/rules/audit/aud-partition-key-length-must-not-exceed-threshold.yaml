# Canonical source: vault 20-engine/rules/规则文档/分区键的长度不得超过阈值.md
# TODO(产品核对): aud-partition-key-length-must-not-exceed-threshold → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-partition-key-length-must-not-exceed-threshold
category: ddl
severity: warning
tags:
- review:CREATE TABLE
content:
  zh:
    name: 分区键的长度不得超过阈值
    description: |-
      如果使用 `CHAR` 或 `VARCHAR` 类型的字段作为分区键，过长的字段值会增加分区计算和哈希路由的成本，并可能导致分区元数据和索引过大，影响分区管理的效率。
      建议分区键的字符串长度不超过 255 个字符。对于更长的字符串值，可以考虑使用其哈希值或更短的业务标识作为分区键。
    badExample: |-
      -- ❌ 不推荐：分区键字段长度超过 255 字符
      CREATE TABLE customers (
          id        BIGINT,
          city_name VARCHAR(500),
          PRIMARY KEY (id, city_name)
      ) PARTITION BY LIST COLUMNS (city_name) (
          PARTITION p_beijing VALUES IN ('Beijing'),
          PARTITION p_shanghai VALUES IN ('Shanghai')
      );
    goodExample: |-
      -- ✅ 推荐：分区键字段长度不超过阈值
      CREATE TABLE customers (
          id        BIGINT,
          city_name VARCHAR(100),
          PRIMARY KEY (id, city_name)
      ) PARTITION BY LIST COLUMNS (city_name) (
          PARTITION p_beijing VALUES IN ('Beijing'),
          PARTITION p_shanghai VALUES IN ('Shanghai')
      );
  en:
    name: Partition Key Length Must Not Exceed Threshold
