# Canonical source: vault 20-engine/rules/规则文档/避免常量字符串开头或结尾包含空格.md
# TODO(产品核对): aud-avoid-leading-or-trailing-spaces-in-string-liter → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(复核): en 正文为 zh（vault 源）的忠实翻译，发布前建议母语/产品复核；Rule ID/category/severity 仍待注册表定稿。


id: aud-avoid-leading-or-trailing-spaces-in-string-liter
category: dml
severity: warning
tags:
- review:SELECT
- review:UPDATE
- review:DELETE
- review:INSERT
content:
  zh:
    name: 避免常量字符串开头或结尾包含空格
    description: |-
      在 SQL 中，字符串常量开头或结尾的空格通常不具有实际的业务意义。这类情况往往是由于开发人员的误输入导致的（例如从文档中复制粘贴时多带了前后空格），但它可能直接影响 SQL 查询的结果——`WHERE name = ' test '` 和 `WHERE name = 'test'` 会匹配到不同的数据。
      开发者应特别关注此类预警，检查字符串常量的前后空格是否为有意为之。如果是误输入，应及时清理；如果确实需要匹配带空格的数据，建议使用 `TRIM()` 函数或添加注释说明意图。
    badExample: |-
      -- ❌ 不推荐：字符串常量包含首尾空格，可能是误输入
      SELECT * FROM t WHERE name = ' test ';
    goodExample: |-
      -- ✅ 推荐：去除无意义的空格
      SELECT * FROM t WHERE name = 'test';

      -- 如果确实需要匹配含空格的数据，使用 TRIM 或显式注明意图
      SELECT * FROM t WHERE TRIM(name) = 'test';
    summary: 字符串常量首尾空格多为误输入却影响匹配结果；应清理，确需匹配空格时用 TRIM 或注释说明。
  en:
    name: Avoid Leading or Trailing Spaces in String Literals
    description: |-
      Leading or trailing spaces in SQL string literals usually carry no business meaning and often come from a typo (for example copy-pasting that drags extra spaces in). But they can change query results directly - `WHERE name = ' test '` and `WHERE name = 'test'` match different data.

      Take these notices seriously and check whether surrounding spaces in a string literal are intentional. If it is a typo, clean it up; if you really must match data with spaces, use `TRIM()` or add a comment stating the intent.
    summary: Spaces at the start or end of string literals are usually typos but change matches; remove them, or use TRIM() when spaces are intended.
    badExample: |-
      -- bad: a string literal with leading/trailing spaces, possibly a typo
      SELECT * FROM t WHERE name = ' test ';
    goodExample: |-
      -- good: drop the meaningless spaces
      SELECT * FROM t WHERE name = 'test';

      -- to actually match data that may carry spaces, use TRIM or state the intent
      SELECT * FROM t WHERE TRIM(name) = 'test';
