# Canonical source: vault 20-engine/rules/规则文档/避免无条件且无分组的SELECT语句.md
# TODO(产品核对): aud-avoid-select-without-where-and-group-by → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-avoid-select-without-where-and-group-by
category: dml
severity: info
tags:
- review:SELECT
content:
  zh:
    name: 避免无条件且无分组的SELECT语句
    description: |-
      没有 WHERE 条件（或条件恒真）且没有 GROUP BY 聚合的 SELECT 语句，会触发全表扫描并返回表中的所有数据行。这种查询在小型表中可能无害，但在大型表中会导致巨大的结果集和 IO 开销，严重影响数据库性能和网络带宽。
      如果业务的确实需要导出全表数据，建议使用分页查询（LIMIT + OFFSET）或数据导出工具来分批处理。对于常规的业务查询，应始终添加合理的过滤条件和聚合逻辑来限制返回的数据量。
    badExample: |-
      -- ❌ 不推荐：无条件无聚合，全表扫描返回所有数据
      SELECT * FROM t;
    goodExample: |-
      -- ✅ 推荐：添加 WHERE 条件限制数据范围
      SELECT * FROM t WHERE created_at >= '2025-01-01';

      -- 或使用 GROUP BY 进行数据聚合
      SELECT dept_id, COUNT(*) FROM t GROUP BY dept_id;
  en:
    name: Avoid SELECT Without WHERE and GROUP BY
