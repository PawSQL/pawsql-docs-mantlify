# Canonical source: vault 20-engine/rules/规则文档/应使用指定的数据分布方式.md
# TODO(产品核对): aud-use-specified-data-distribution-method → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-use-specified-data-distribution-method
category: ddl
severity: warning
tags:
- review:CREATE TABLE`（分布式环境）
content:
  zh:
    name: 应使用指定的数据分布方式
    description: |-
      在TDSQL等分布式数据库中，应使用指定的数据分布方式（默认为`murmurhash`算法）作为分片键的哈希函数。`murmurhash`是一种非加密型哈希函数，具有高运算效率和良好的分布均匀性，能够确保数据在各个分片上尽可能均匀分布，避免数据倾斜导致的节点负载不均。
      使用其他非标准的分布方式可能导致数据分布不均，某些分片的数据量远大于其他分片，从而引发热点问题、跨节点查询性能下降，严重时甚至导致单个节点成为系统瓶颈。
    badExample: ''
    goodExample: |-
      -- ✅ 推荐：使用默认的murmurhash分布算法，数据均匀分布在各个分片
      CREATE TABLE t (
          id BIGINT,
          name VARCHAR(100)
      ) shardkey=id;
  en:
    name: Use Specified Data Distribution Method
