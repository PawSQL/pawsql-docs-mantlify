# Canonical source: vault 20-engine/rules/规则文档/SQL长度超过阈值.md
# TODO(产品核对): aud-sql-length-exceeds-threshold → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-sql-length-exceeds-threshold
category: dml
severity: info
tags:
- review:SELECT
- review:INSERT
- review:UPDATE
- review:DELETE
content:
  zh:
    name: SQL长度超过阈值
    description: |-
      过长的 SQL 语句可读性较差，难以维护和调试；同时，过长的 SQL 可能包含过多的表连接或子查询嵌套，容易引发性能问题。在团队协作和代码审查中，冗长的 SQL 增加了理解成本和出错概率。
      PawSQL 检测 SQL 文本的总长度是否超过用户指定的阈值（默认 1024），对超过阈值的 SQL 进行预警，引导开发者将复杂查询拆分为更小、更可维护的单元。
    badExample: |-
      -- ❌ 不推荐：SQL 语句过长，可读性和维护性差
      SELECT /* 一段非常长的 SQL 包含多个子查询和复杂逻辑，超过阈值... */;
    goodExample: |-
      -- ✅ 推荐：将复杂 SQL 拆分为多个步骤或使用 CTE
      WITH step1 AS (...), step2 AS (...) SELECT ...;
  en:
    name: SQL Length Exceeds Threshold
