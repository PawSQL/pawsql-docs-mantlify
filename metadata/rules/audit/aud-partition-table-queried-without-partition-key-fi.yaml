# Canonical source: vault 20-engine/rules/规则文档/访问分区表没有使用分区字段过滤.md
# TODO(产品核对): aud-partition-table-queried-without-partition-key-fi → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-partition-table-queried-without-partition-key-fi
category: dml
severity: info
tags:
- review:SELECT
- review:UPDATE
- review:DELETE
content:
  zh:
    name: 访问分区表没有使用分区字段过滤
    description: |-
      在访问分区表时，如果查询条件中没有使用分区字段进行过滤，数据库将不得不扫描所有分区来定位数据，即发生"全分区扫描"。这不仅无法发挥分区表的"分区裁剪"优势，还会导致查询性能显著下降、IO 开销大幅增加，尤其是在分区数量较多或单分区数据量较大的场景下。
      正确使用分区字段过滤，可以让优化器精准定位到仅需访问的目标分区，大幅减少扫描范围，显著提升查询效率。该规则是分区表查询优化的核心规范之一。
    badExample: |-
      -- ❌ 不推荐：访问分区表未使用分区键过滤，导致全分区扫描
      SELECT * FROM partitioned_table WHERE non_partition_col = 1;
    goodExample: |-
      -- ✅ 推荐：使用分区键作为过滤条件，实现分区裁剪
      SELECT * FROM partitioned_table WHERE partition_key = '2025-01-01';
  en:
    name: Partition Table Queried Without Partition Key Filter
