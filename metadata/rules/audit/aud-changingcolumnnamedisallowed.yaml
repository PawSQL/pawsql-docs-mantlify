# Canonical source: vault 20-engine/rules/规则文档/禁止修改字段名.md
# TODO(产品核对): aud-changingcolumnnamedisallowed → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-changingcolumnnamedisallowed
category: ddl
severity: warning
tags:
- review:ALTER TABLE RENAME COLUMN
content:
  zh:
    name: 禁止修改字段名
    description: |-
      禁止修改字段名（`RENAME COLUMN` / `CHANGE COLUMN`）。重命名字段会对所有依赖该字段的应用程序、报表、ETL 脚本和视图造成影响，导致线上业务瞬间中断。在不支持不停机发布的架构中，这种修改需要在维护窗口内协调大量上下游系统，风险极高。
      如果确实需要重命名字段，应由 DBA 在充分评估依赖关系后手工干预，通过灰度方案（如新增列 + 双写 + 切换 + 下线旧列）实现平滑过渡，而不是直接执行 `RENAME` 操作。
    badExample: |-
      -- ❌ 不推荐：直接重命名列，破坏上下游依赖
      ALTER TABLE t CHANGE old_name new_name VARCHAR(50);
    goodExample: |-
      -- ✅ 推荐：灰度方案
      -- 1. ALTER TABLE t ADD COLUMN new_name VARCHAR(50);
      -- 2. 双写 + 历史数据回填
      -- 3. 切换所有消费方使用 new_name
      -- 4. ALTER TABLE t DROP COLUMN old_name;
  en:
    name: ChangingColumnNameDisallowed
