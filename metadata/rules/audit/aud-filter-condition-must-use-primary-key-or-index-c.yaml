# Canonical source: vault 20-engine/rules/规则文档/过滤条件中必须使用主键或索引列.md
# TODO(产品核对): aud-filter-condition-must-use-primary-key-or-index-c → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-filter-condition-must-use-primary-key-or-index-c
category: dml
severity: info
tags:
- review:SELECT
- review:UPDATE
- review:DELETE
content:
  zh:
    name: 过滤条件中必须使用主键或索引列
    description: |-
      当 SQL 语句的 WHERE 或 JOIN 条件中的列缺少可利用的主键或索引时，数据库优化器只能选择全表扫描来定位数据。全表扫描会逐行读取整个表的数据，随着表数据量的增长，查询性能将线性恶化，严重影响系统吞吐量和响应时间。
      确保过滤条件命中索引或主键，是数据库查询优化的基本准则。如果某个查询没有合适的索引可用，应及时考虑创建匹配的索引来支撑该查询。
    badExample: |-
      -- ❌ 不推荐：过滤条件未命中任何索引，导致全表扫描
      SELECT * FROM t WHERE unindexed_col = 1;
    goodExample: |-
      -- ✅ 推荐：过滤条件使用了索引列，可利用索引快速定位
      SELECT * FROM t WHERE indexed_col = 1;
  en:
    name: Filter Condition Must Use Primary Key or Index Column
