# Canonical source: vault 20-engine/rules/规则文档/必须显式的指定分布键.md
# TODO(产品核对): aud-explicit-distribution-key-required → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-explicit-distribution-key-required
category: ddl
severity: warning
tags:
- review:CREATE TABLE`（分布式环境）
content:
  zh:
    name: 必须显式的指定分布键
    description: |-
      在分布式数据库中，如果建表时未显式指定分布键（`DISTRIBUTED BY` / `SHARDKEY` / `DISTRIBUTE BY`），数据库将采用默认策略（如随机分布或按首列分布）自动选择分布键。这种"隐式"行为看似便捷，实则暗藏风险：自动选择的分布键未必是最优的，可能导致数据分布不均（数据倾斜）、频繁的跨节点数据重分布、以及关键查询退化为全节点扫描，严重影响查询性能与集群稳定性。
      显式指定分布键，意味着开发者基于业务语义主动声明数据的分布策略，确保数据在各节点间均匀分布、关联操作本地化执行，从而充分利用分布式数据库的并行计算能力。该规则是分布式数据库表设计的核心规范之一。
    badExample: |-
      -- ❌ 不推荐：未显式指定分布键，依赖数据库默认策略
      CREATE TABLE orders (
          order_id    BIGINT NOT NULL,
          customer_id BIGINT NOT NULL,
          order_date  DATE   NOT NULL
      );
    goodExample: |-
      -- ✅ 推荐：显式指定分布键（以 openGauss/PostgreSQL-XL 语法为例）
      CREATE TABLE orders (
          order_id    BIGINT NOT NULL,
          customer_id BIGINT NOT NULL,
          order_date  DATE   NOT NULL
      ) DISTRIBUTED BY HASH(order_id);

      -- ✅ 推荐：显式指定分布键（以 TDSQL 语法为例）
      CREATE TABLE orders (
          order_id    BIGINT NOT NULL,
          customer_id BIGINT NOT NULL,
          order_date  DATE   NOT NULL
      ) shardkey=order_id;
  en:
    name: Explicit Distribution Key Required
