# Canonical source: vault 20-engine/rules/规则文档/模式命名规范.md
# TODO(产品核对): aud-schema-naming-convention → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: aud-schema-naming-convention
category: ddl
severity: info
tags:
- review:CREATE SCHEMA
content:
  zh:
    name: 模式命名规范
    description: |-
      此规则定义了数据库模式（schema）的命名规范。统一、一致的命名规范可以显著提升数据库对象的可读性和可维护性，降低团队成员之间的沟通成本。在大型项目或多团队协作环境中，不规范的命名会导致理解困难、脚本冲突和运维失误。
      例如，模式名使用小写字母和连字符（kebab-case），使名称简洁且跨平台兼容。违反此规则的创建操作应进行调整。
    badExample: |-
      -- ❌ 不推荐：模式名不符合命名规范
      CREATE SCHEMA "MySchema";
    goodExample: |-
      -- ✅ 推荐：使用小写字母和连字符，符合 kebab-case 规范
      CREATE SCHEMA my_schema;
  en:
    name: Schema Naming Convention
