# Canonical source: vault 20-engine/rules/规则文档/修饰子查询重写优化.md
# TODO(产品核对): opt-anysomeall-subquery-rewrite-optimization → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: opt-anysomeall-subquery-rewrite-optimization
category: rewrite
severity: info
tags:
- review:SELECT
content:
  zh:
    name: 修饰子查询重写优化
    description: |-
      `ANY`/`SOME`/`ALL` 修饰的子查询源自 SQL-92 标准，通常用于检查某个值与子查询返回的全部值或任意值的大小关系（如 `x < ALL(subquery)` 或 `x > ANY(subquery)`）。使用这类修饰子查询的执行效率低下，因为数据库需要对子查询的结果集逐行进行比较，随着结果集大小的增加，性能线性下降。
      通过查询重写可以将 `ANY`/`SOME`/`ALL` 修饰的子查询转换为等价的 `MIN`/`MAX` 聚合子查询，配合 `LIMIT` 可以有效利用索引加速。PawSQL 支持针对 MySQL、PostgreSQL、Oracle 等不同数据库自动生成最优的重写方案。
    badExample: |-
      -- ❌ 优化前：ALL 修饰的子查询，需要逐行比较
      SELECT *
      FROM orders
      WHERE o_orderdate < ALL (
          SELECT o_orderdate FROM orders WHERE o_custkey > 100
      );
    goodExample: |-
      -- ✅ 优化后（MySQL）：重写为 MIN + LIMIT，利用索引加速
      SELECT *
      FROM orders
      WHERE o_orderdate < (
          SELECT o_orderdate
          FROM orders
          WHERE o_custkey > 100
          ORDER BY o_orderdate ASC
          LIMIT 1
      );
  en:
    name: ANY/SOME/ALL Subquery Rewrite Optimization
