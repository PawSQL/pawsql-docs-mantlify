# Canonical source: vault 20-engine/rules/规则文档/无条件的DELETE建议重写为Truncate.md
# TODO(产品核对): opt-rewrite-unconditional-delete-to-truncate → 规则注册表 Rule ID / category / severity 定稿后再发布。
# TODO(内容): 英文正文（description/whyItMatters/howToFix/示例注释）待人工补齐；zh 由源文档提取，需复核。

id: opt-rewrite-unconditional-delete-to-truncate
category: rewrite
severity: info
tags:
- review:DELETE
content:
  zh:
    name: 无条件的DELETE建议重写为Truncate
    description: |-
      没有WHERE条件或WHERE条件恒真的`DELETE`语句会删除表中的所有数据。`DELETE`作为DML语句，需要逐行记录日志以便事务回滚和主备同步——对于大表而言，这可能导致数据库长时间锁定、事务日志膨胀、主从同步延迟等问题。
      如果确认表中的数据不再需要，应当使用`TRUNCATE`来代替无条件的`DELETE`。`TRUNCATE`是DDL操作，不会逐行记录日志，而是直接将表清空并释放存储空间，执行速度远快于`DELETE`，且对系统资源的消耗极小。
    badExample: |-
      -- ❌ 不推荐：无条件的DELETE，逐行删除并记录日志，大表场景极其低效
      DELETE FROM lineitem;
    goodExample: |-
      -- ✅ 推荐：使用TRUNCATE直接清空表，速度快且不产生行级日志
      TRUNCATE TABLE lineitem;
  en:
    name: Rewrite Unconditional DELETE to TRUNCATE
