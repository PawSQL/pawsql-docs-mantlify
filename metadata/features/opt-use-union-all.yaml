# Pilot mapping data (design §9). Phase-3 gate consumes these.
# Feature for canonical optimizer rule: Use UNION ALL Instead of UNION.
id: OPT-USE-UNION-ALL
name: Use UNION ALL Instead of UNION
product: pawsql
status: released
documentation:
  reference:
    required: true
    paths: []
    documents:
    - documentId: optimizer-rule-opt-use-union-all-instead-of-union
      language: zh
components:
- optimizer
