# SAMPLE — example metadata, verify versions/features against the product.
database: mysql
title: MySQL
supportedVersions:
  - "5.7"
  - "8.0"
features:
  optimizer: true
  audit: true
  planVisualizer: false

description: >-
  PawSQL supports MySQL for index recommendation, SQL rewrite and audit-rule
  review across the supported release lines.

keyFeatures:
  - Cost-based index recommendation
  - Semantic query rewrite
  - SQL audit rule review

notes: >-
  Version list is sample data pending confirmation from the product team.
