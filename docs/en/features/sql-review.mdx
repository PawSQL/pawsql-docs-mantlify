---
id: en-feature-sql-review
title: SQL Review
description: Detect SQL quality, risk, compatibility, and potential performance issues
  before SQL reaches testing, release, or production.
type: explanation
product: pawsql
status: draft
tags:
- features
- sql-review
language: en
translationKey: feature-sql-review
localeOf: feature-sql-review
layout: detail
capabilities:
- sql-audit
---

PawSQL SQL Review helps teams detect SQL issues early in the lifecycle, including syntax and compatibility problems, policy violations, risky DDL/DML, and patterns that may lead to performance degradation.

Instead of relying only on text matching or regular expressions, PawSQL combines SQL parsing, semantic analysis, database metadata, and database-specific rules to perform structured SQL review.


<style>
.paw-flow {
  margin: 24px 0 32px;
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}
.paw-flow * { box-sizing: border-box; }
.paw-flow .flow-row {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: center;
}
.paw-flow .flow-node {
  min-width: 150px;
  padding: 14px 16px;
  border: 1px solid #d9e2ec;
  border-radius: 12px;
  background: #ffffff;
  color: #102a43;
  font-weight: 650;
  text-align: center;
  box-shadow: 0 1px 2px rgba(16,42,67,.05);
}
.paw-flow .flow-node.primary {
  border-color: #bfdbfe;
  background: #eff6ff;
  color: #1d4ed8;
}
.paw-flow .flow-node.soft {
  background: #f7fafc;
}
.paw-flow .flow-arrow {
  color: #9fb3c8;
  font-size: 20px;
  line-height: 1;
  font-weight: 700;
}
.paw-flow .flow-grid {
  display: grid;
  gap: 12px;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  margin-top: 14px;
}
.paw-flow .flow-card {
  border: 1px solid #d9e2ec;
  border-radius: 12px;
  padding: 14px;
  background: #ffffff;
  color: #102a43;
}
.paw-flow .flow-card strong {
  display: block;
  margin-bottom: 5px;
}
.paw-flow .flow-card span {
  color: #627d98;
  font-size: 13px;
  line-height: 1.55;
}
@media (max-width: 760px) {
  .paw-flow .flow-row,
  .paw-flow .flow-grid {
    display: grid;
    grid-template-columns: 1fr;
  }
  .paw-flow .flow-arrow {
    transform: rotate(90deg);
    text-align: center;
  }
}
</style>


## Overview

Traditional SQL review often depends on manual DBA checks, regular expressions, or lightweight linting. These approaches have difficulty understanding complex SQL structures, object relationships, database semantics, stored procedures, and heterogeneous database environments.

PawSQL separates SQL review into multiple analysis stages:

<div class="paw-flow">
  <div class="flow-row">
    <div class="flow-node">SQL Input</div><div class="flow-arrow">→</div>
    <div class="flow-node">Parse & Normalize</div><div class="flow-arrow">→</div>
    <div class="flow-node">Semantic Analysis</div><div class="flow-arrow">→</div>
    <div class="flow-node primary">Review Rules</div><div class="flow-arrow">→</div>
    <div class="flow-node">Risk Report</div>
  </div>
</div>

## Why SQL Review Matters

The later a SQL issue is discovered, the more expensive it usually becomes to fix.

A full-table scan discovered while coding can be corrected immediately. The same SQL discovered after production deployment may cause CPU pressure, excessive I/O, lock contention, or release rollback.

The purpose of SQL Review is therefore not simply to find mistakes, but to shift SQL quality control earlier into engineering and delivery workflows.

## Key Capabilities

### SQL syntax and compatibility checks

Detect syntax errors, dialect differences, and SQL constructs that are not supported by the target database.

Typical scenarios include:

- Post-migration compatibility checks
- Multi-database product development
- Developer self-service validation

### SQL coding policy review

Check SQL against organizational or team-level policies, such as:

- Discouraged SQL patterns
- Table, column, and index conventions
- SELECT / UPDATE / DELETE constraints
- DDL change policies
- SQL maintainability rules

### High-risk DDL and DML detection

Detect operations that may have a significant impact on data safety or production stability, including:

- UPDATE / DELETE without filters
- TRUNCATE
- DROP TABLE
- High-risk ALTER TABLE operations
- Operations that may hold large locks

### Performance issue detection

Identify potential performance risks before deployment, such as:

- Full table scans
- Non-SARGable predicates
- Ineffective index usage
- Unnecessary sorting and aggregation
- Cartesian joins
- Inefficient subquery and join patterns

### Database-specific rules

Apply database-aware rules rather than using one generic rule set for every database.

This is especially important across Oracle, MySQL, PostgreSQL, SQL Server, DB2, domestic databases, and distributed databases.

### Stored procedure analysis

PawSQL can analyze SQL embedded inside stored procedures, extending SQL Review beyond standalone SQL statements into procedural SQL such as T-SQL and PL/pgSQL.

## Example

The following SQL is syntactically valid but operationally risky:

```sql
UPDATE orders
SET status = 'CLOSED';
```

PawSQL can identify that:

- The UPDATE statement has no filter condition
- A large number of rows may be affected
- The operation may create a long-running transaction or broad locking impact

Review results can be mapped to severity levels such as:

| Level | Meaning | Typical action |
|---|---|---|
| Info | Advisory policy issue | Developer review |
| Warning | Potential risk | Recommended change |
| Critical | High-risk operation | Block release |

## Review in the Development Lifecycle

SQL Review can be used through multiple entry points:

- IDE: instant developer feedback
- Web Console: manual SQL review
- CI/CD: SQL Quality Gate
- API: enterprise platform integration
- Webhook: DevOps workflow integration
- MCP: SQL review capability for AI agents

## SQL Review vs. Simple SQL Lint

| Capability | Simple SQL Lint | PawSQL SQL Review |
|---|---|---|
| Syntax check | ✓ | ✓ |
| AST analysis | Limited | ✓ |
| Database metadata | Usually no | ✓ |
| Database-specific rules | Limited | ✓ |
| Performance rules | Limited | ✓ |
| Risk classification | Limited | ✓ |
| Stored procedure analysis | Usually no | ✓ |
| CI/CD governance | Depends | ✓ |

## Related Features

- [Automatic SQL Rewrite](/en/features/automatic-sql-rewrite)
- [Index Recommendation](/en/features/index-recommendation)
- [Performance Validation](/en/features/performance-validation)
- [Execution Plan Visualization](/en/features/execution-plan-visualization)

## Related Use Cases

- Developer SQL Copilot
- SQL Quality Gate for CI/CD
- DBA Batch SQL Governance
- Database Migration SQL Governance

## Next Steps

Continue with:

- [SQL Quality Gate](/en/features/sql-quality-gate)
- [Supported Databases](/en/getting-started/supported-databases)
